# Production stack — see docker-compose.prod.yml for services and docs.
# EC2: docker compose -f docker-compose.prod.yml up -d --build
#
# Other compose files:
#   docker-compose.dev.yml    — local full stack (Postgres + Redis + frontend)
#   docker-compose.infra.yml  — local infra only (npm run dev on host)

include:
  - path: docker-compose.prod.yml
