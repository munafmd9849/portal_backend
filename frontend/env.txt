# Frontend Environment Variables
# ===============================

# API Base URL
# Production: Vercel Backend URL
VITE_API_URL=https://portal-backend-orcin.vercel.app/api

# Socket.IO URL
# Production: Vercel Backend URL (same as API)
VITE_SOCKET_URL=https://portal-backend-orcin.vercel.app

# Frontend URL (optional)
# Used for OAuth redirects and link generation
# Update this to your production frontend URL when deployed
VITE_FRONTEND_URL=http://localhost:5173

# Note: All Vite environment variables must be prefixed with VITE_
# They are exposed to the client-side code, so don't include sensitive secrets

# Development Override (for local development):
# To use localhost backend during development, uncomment these:
# VITE_API_URL=http://localhost:3000/api
# VITE_SOCKET_URL=http://localhost:3000
