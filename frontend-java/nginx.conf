server {
    listen 80;
    listen [::]:80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Serve real assets directly; route all other requests through the React SPA.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
