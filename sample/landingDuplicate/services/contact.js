/**
 * Sample mini-project contact service.
 * Keeps the contact form working without depending on the main app backend.
 */

export async function submitContactForm(_formData) {
  // If you want to wire this to a real backend later, replace this stub.
  return {
    success: true,
    message: 'Sample mode: message captured (no backend call).',
  };
}

